menu "Musopti Audio"
    config MUSOPTI_AUDIO_SIMULATED
        bool "Use simulated audio (log only, no I2S)"
        default y
        help
            When enabled, tone playback is simulated via log messages.
            Disable this when building for the real board with ES8311.
endmenu
